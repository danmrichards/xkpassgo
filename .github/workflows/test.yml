name: Go
on: 
  pull_request:
  push:
    branches:    
      - master
jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
    - name: Set up Go
      uses: actions/setup-go@v1
      with:
        go-version: 1.13
      id: go 
    - name: Test
      run: go test -v -race ./...
