package generator

import "testing"

func TestGeneratorConfig_Validate(t *testing.T) {
	// TODO: TestGeneratorConfig_Validate
	tests := []struct {
		name string
	}{}
	for _, tc := range tests {
		t.Run(tc.name, func(t *testing.T) {

		})
	}
}
